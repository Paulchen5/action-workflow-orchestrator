name: Project Setup

description: "Set up the Project"

runs:
  using: "composite"
  steps:
    - name: Set up Node.js with node-version 22
      uses: actions/setup-node@v6
      with:
        node-version: 22

    - name: Install dependencies
      shell: bash
      run: npm clean-install
